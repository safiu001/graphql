export type coinInputType = {
    skip: number,
    limit: number,
    currency: string
}

export type newsInputType = {
    skip: number,
    limit: number,
    toDate: string,
    fromDate: string
}